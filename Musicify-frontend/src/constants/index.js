// Update the API_ENDPOINTS to ensure correct URL
export const API_ENDPOINTS = {
    // Make sure this points to your backend server
    FIND_SONG: 'http://localhost:5000/api/song/find', // Adjust port if needed
    // Other endpoints...
};